name: Wake HotShort PC

on:
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest

    steps:
      - name: Install curl
        run: sudo apt-get update && sudo apt-get install -y curl

      - name: Wake PC via phone relay
        run: curl http://100.122.45.112:8080/wakepc
